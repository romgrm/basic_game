name: basic_game
description: A basic game for test flutterFlame plugin
publish_to: "none"

version: 1.0.0+1

environment:
  sdk: ">=3.1.2 <4.0.0"

dependencies:
  flutter:
    sdk: flutter
  flame: 1.10.0
  cupertino_icons: ^1.0.2

dev_dependencies:
  flame_test: ^1.10.1
  flutter_test:
    sdk: flutter
  flutter_lints: ^2.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/images/